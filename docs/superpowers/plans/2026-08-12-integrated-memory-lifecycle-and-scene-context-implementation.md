# Integrated Memory Lifecycle and Scene Context Implementation

1. Add scene-context settings, deterministic extraction, record enrichment, and chapter range derivation.
2. Stop sending the complete assistant output to the narrative-summary model.
3. Migrate the ordinary turn/chapter UI to visible-floor narrative data.
4. Add stable fact source coordinates and best-effort legacy backfill.
5. Extend lifecycle data with dormant/historical archives, staged organization, snapshots, adoption, rollback, and guarded incremental review.
6. Remove the unsafe first-open full audit trigger and update prompts to enforce post-establishment evidence.
7. Add dormant retrieval to core memory composition without mutating the active table.
8. Cover scene extraction, L2 input isolation/UI, source coordinates, lifecycle staging/adoption/rollback, and branch safety with smoke tests.
9. Bump the extension version, run the full test suite, commit, and push.
10. Snapshot the installed extension, saved/runtime V3.35, and Branch #349; deploy; synchronize preset/runtime, scene rule, recent raw allowance, and the two pinned ownership corrections.
11. Verify installed commit/version, HTTP assets, live settings parity, chat hashes, scene backfill counts, L1/L2 preview, and rollback artifacts.

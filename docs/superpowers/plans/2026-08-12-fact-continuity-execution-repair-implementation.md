# Fact Continuity and Prose Execution Repair Plan

1. Change per-floor L2 rendering to emit the complete summary before supplemental structured segments.
2. Add regression assertions for a summary whose decisive attribution is absent from its validated segment.
3. Bump the extension patch version and update release documentation.
4. Snapshot the live Branch #349 JSONL, V3.35 preset, and runtime settings.
5. Apply an atomic, hash-guarded in-place repair to the two live ownership facts with manual protection and audit records.
6. Add the reusable V3.35 fact-anchor Step and final silent prose verification to both saved preset and runtime settings without changing Chat History or the memory macro.
7. Run the full local suite, deploy by fast-forward, run the server suite, and perform read-only semantic acceptance checks.
